# Optional: set your production URL (include https://, no trailing slash).
# On Vercel you can skip this; VERCEL_URL is used for metadata and sitemap at build time.
# Use this when you assign a custom domain so Open Graph URLs stay correct.
# NEXT_PUBLIC_SITE_URL=https://your-domain.com
